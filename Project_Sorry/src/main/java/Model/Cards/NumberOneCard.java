package Model.Cards;

/**
 * With this card, a pawn can start from the Start position.
 * Alternatively, the player can move a pawn one position forward (according to the clockwise direction).
 * @author Nikolas Lazaridis
 * @version 1.0
 */
public class NumberOneCard extends NumberCard{
    /**
     *<b>constructor</b>:Constructs a new instance of card 1 and via the
     *parent class NumberCard sets with the command super,
     *value=1 and name=1.
     */
    public NumberOneCard() {
        super(1);
    }




}
